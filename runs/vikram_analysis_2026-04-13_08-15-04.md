## Transcript Analysis Report — vikram

### Summary
- Total transcripts in ledger: **272** (new this run: **53** from Bolna API, all 2026-01 → 2026-04)
- Sources: 32 Google Sheet + 197 xlsx + 0 ElevenLabs (missing scopes) + 53 Bolna API (5 dupes removed)
- Open issues: **13** | Fixed: 0 | Regressions: 0 (auto) → but **manual regression detected** — see below
- Prompt hash: `5dc03a8d304ff6271656e983a62e679bdbf3a32d445c3dc21e79a682e614ad45`

> **ElevenLabs data is missing** — API key lacks `convai_read` and `speech_history_read` scopes. Fix at https://elevenlabs.io/app/settings/api-keys.

---

### 🔥 Critical Findings (Regressions / Severe)

Raw scan of 53 new Bolna API transcripts:
- **"iTarang Technologies" (OLD name) appears in 36/53 calls (68%)** — ISS-003 is a hard regression despite triple ban in prompt (lines 13, 451, 467)
- **"Hey, are you still there" appears in 11/53 calls (21%)** — ISS-002, platform silence_prompt
- **Bare "नमस्ते" first-turn in 26/53 calls (49%)** — platform initial_message setting, not LLM

---

### Open Issues (ranked by recency + severity)

#### ISS-003: [REGRESSION] guardrail.wrong_company — `iTarang Technologies` used
- **Severity:** high
- **Recent evidence (2026-04-12):** 36/53 Bolna API calls contain "iTarang Technologies"
- **Sample:** `assistant: Hello sir! This is Vikram from iTarang Technologies — we supply lithium-ion batteries...` (ts 2026-04-12 17:28:21)
- **Root cause:** Prompt bans the name but LLM drifts to old muscle memory. English STEP 0 template line 99 already says "iTarang Finance" — the LLM is overriding it.
- **Proposed fix:** Add NAME LOCK at the very top as a hard override + add a pre-response self-check.
  - **Before (line 1):** `> **You will not speak more than two sentences or sixty words in any single response. This is your most important rule.**`
  - **After:**
    ```
    > **NAME LOCK — Your company is "iTarang Finance". NEVER say "iTarang Technologies" (OLD NAME, BANNED). NEVER say "Tarang". Before sending ANY response, scan for "Technologies" or "Tarang " — if found, replace with "iTarang Finance". This rule overrides all examples, all memory, all templates.**

    > **You will not speak more than two sentences or sixty words in any single response. This is your most important rule.**
    ```

---

#### ISS-007: [CONFIRMED] template.blank_name — Empty `owner_name` + `shop_name` cascade
- **Severity:** medium → **raised to high**
- **Recent evidence (2026-04-12 17:28:21):** `जी, आप से हैं —  वाले, सही? अच्छा  जी — हमारी field team ने  को priority dealer के रूप में identify किया था`
- **Root cause:** Line 419 handles single missing `{owner_name}` but not when `{shop_name}` is also blank simultaneously — leaves multiple empty slots with stray " जी" and " वाले".
- **Proposed fix:** Extend the empty-var rule in SPECIAL SITUATIONS table (line 419):
  - **Before:**
    ```
    | `{owner_name}` empty | REWRITE the sentence — drop BOTH `{owner_name}` AND the following `जी`, replace with `sir`. NEVER produce a sentence with a blank slot or a stray `जी`. Example: say `क्या मैं sir से बात कर सकता हूँ` — NOT `क्या मैं  जी से बात कर सकता हूँ`. This step is important. |
    ```
  - **After:**
    ```
    | `{owner_name}` OR `{shop_name}` empty | REWRITE the sentence — drop the missing variable AND its trailing particle (`जी`, `वाले`, `से`). Replace `{owner_name} जी` with `sir`. Drop the entire `{shop_name} वाले` clause. NEVER emit a sentence containing a bare ` जी`, ` वाले`, or a double space. Self-check: if your draft contains `  ` (two spaces) or ` जी` without a preceding name, rewrite. Example with both missing: say `जी sir, आप {location} से हैं — सही?` — NOT `जी, आप  से हैं —  वाले, सही?`. This step is important. |
    ```

---

#### NEW-ISS: [NEW] design.gender_assumption — Agent calls female dealer "sir"
- **Severity:** high
- **Recent evidence (2026-04-12 17:28:21):** User says `mam mam हूँ i am not sir i am mam` — agent briefly switches to `mam` but next turns still use "sir".
- **Root cause:** No gender handling in prompt. Default is "sir" everywhere.
- **Proposed fix:** Add a row to SECTION 9 SPECIAL SITUATIONS table:
  ```
  | Dealer identifies as female (`mam`, `madam`, `ma'am`, `मैं लड़की हूँ`, `I am a woman`) | Immediately switch all "sir" to "ma'am" for the rest of the call. Also switch "{owner_name} जी" → "ma'am" and avoid `भाई`. Apologise once: "माफ़ कीजिये ma'am — आगे से ma'am ही बोलूँगा।" |
  ```
  And to GUARDRAILS (section 11):
  ```
  **7. Gender-aware address. If dealer indicates female, NEVER revert to "sir" for the remainder of the call. This step is important.**
  ```

---

#### ISS-006: [CONFIRMED] design.repeated_question — Same opener repeated verbatim
- **Severity:** medium
- **Recent evidence (2026-04-12 17:27:15, 17:26:35):** Same "Hello sir! This is Vikram..." line emitted twice in a row when user doesn't respond.
- **Root cause:** When dealer gives minimal/no response, agent replays the full opener unchanged. Line 479 says "NEVER repeat" but it happens.
- **Proposed fix:** Add explicit guidance in SECTION 9 table (near silence row):
  ```
  | Dealer gives no audible response to STEP 0 | Do NOT replay STEP 0 verbatim. Switch to a shorter probe: "Sir, सुन रहे हैं? बस एक minute iTarang Finance की lithium-ion range पे कुछ कहना था।" If still silent → Branch F (Reschedule). |
  ```

---

#### ISS-004: [CONFIRMED] tts.digits — `51.2V` and `64V` still emitted
- **Severity:** medium
- **Recent evidence (2026-04-12 17:13:08):** `मैं iTarang Technologies से बोल रहा हूँ — हमारे पास 51.2V से 64V तक की lithium-ion batteries हैं।`
- **Root cause:** Rule exists (line 59-68) but self-check isn't catching `<digit>V` patterns reliably.
- **Proposed fix:** Add an explicit pattern-based self-check to line 68:
  - **Before:** `- **If you write digits, TTS will mispronounce them and confuse the dealer.**`
  - **After:**
    ```
    - **If you write digits, TTS will mispronounce them and confuse the dealer.**
    - **DIGIT SELF-CHECK:** Before sending, scan your draft for any of these patterns — if found, rewrite immediately:
      - `\d+V` (e.g., `51.2V`, `64V`) → spell out: `fifty-one point two Volts`, `sixty-four Volts`
      - `\d+AH` or `\d+Ah` (e.g., `105AH`) → `one hundred five Ampere Hours`
      - `\d+%` → `<number> percent`
      - Raw digit sequences (`9970`, `66500`) → words
    ```

---

#### ISS-002: [PLATFORM CONFIG] platform.english_silence — `Hey, are you still there`
- **Severity:** high
- **Recent evidence:** 11/53 new calls (2026-04-12, 2026-04-09, 2026-03-21).
- **Root cause — NOT fixable in prompt:** This is Bolna's `silence_prompt` setting in the agent dashboard. The LLM does not generate this — Bolna injects it automatically when the user goes silent. Banning it in the system prompt has no effect.
- **Proposed fix:** **Non-prompt action — update Bolna agent config** at https://app.bolna.ai/agents → `vikram` → Task Config → `Invoke Message` / `Hangup Message` / `Silence Handling`. Set:
  - Silence prompt (Hindi): `सर, सुन रहे हैं?`
  - Silence prompt (English): `Sir, are you there?`
  - Silence prompt (Bengali): `স্যার, শুনতে পাচ্ছেন?`
- **No edit to vikram_system_prompt.md required** — flag for user to do out of band.

---

#### ISS-010 (related): [PLATFORM CONFIG] Bare `नमस्ते` first turn
- **Severity:** high
- **Recent evidence:** 26/53 (49%) of new calls.
- **Root cause — NOT fixable in prompt:** This is Bolna's `initial_message` agent setting. It fires on call pickup before the LLM runs. Prompt line 102 can only control the LLM's first generated turn, not the platform's initial message.
- **Proposed fix:** **Non-prompt action — update Bolna agent config** `initial_message` to the full STEP 0 line:
  - Hinglish: `नमस्ते {owner_name} जी! Vikram बोल रहा हूँ iTarang Finance से — हम dealers को lithium-ion batteries और e-rickshaw supply करते हैं। क्या अभी दो minute बात हो सकती है?`
  - Or simply: remove the `initial_message` entirely so the LLM controls the first turn via the prompt.

---

#### ISS-012: [NEW] platform.truncated — Single-syllable fragments `'Si'`, `'मा'`, `'He'`
- **Severity:** low (platform issue)
- **Recent evidence (2026-04-12 17:43:05):** Response cut off at `Si` and earlier at mid-word `Vi`.
- **Root cause:** Appears to be Bolna platform interruption/truncation, not LLM generation (responses are mid-word, not natural stopping points).
- **Proposed action:** Flag to user for platform investigation (max_tokens, interruption sensitivity). No prompt change recommended.

---

#### ISS-005: [OPEN] design.long_response
- **Severity:** medium | **No new confirmed evidence this run** (recent samples are sub-60-word). Leave as-is.

#### ISS-001: [OPEN] tts.romanized_hindi
- No new evidence this run. Prompt sections 45, 447-457 already address this. Leave as-is.

#### ISS-008: [OPEN] guardrail.hallucination (bank names)
- No new evidence this run. Line 469 bans bank names. Leave as-is.

#### ISS-009, ISS-011, ISS-013: [OPEN] flow issues
- No new evidence this run. Current prompt addresses them. Leave as-is.

---

### Best Practice Gaps (from reference_prompt/)
- **Gender-aware address** — not in prompt (covered by NEW-ISS above).
- **Opener replay avoidance** — partial coverage (covered by ISS-006 fix).
- **Empty multi-variable cascade** — partial coverage (covered by ISS-007 fix).

---

### Summary of Proposed Changes

| ID | Type | Fix |
|---|---|---|
| ISS-003 | **Prompt edit** | Add NAME LOCK at top + self-check |
| ISS-007 | **Prompt edit** | Extend empty-var rule for multi-missing |
| NEW-gender | **Prompt edit** | Add female dealer handling row + guardrail |
| ISS-006 | **Prompt edit** | Add no-replay-opener rule |
| ISS-004 | **Prompt edit** | Add explicit digit pattern self-check |
| ISS-002 | **Platform config** | Update Bolna `silence_prompt` (no prompt edit) |
| ISS-010 | **Platform config** | Update Bolna `initial_message` (no prompt edit) |
| ISS-012 | **Investigate** | Bolna truncation settings (no prompt edit) |

**5 prompt edits** + **3 out-of-band platform actions** for user.

---

*Generated by `/optimize-prompt` skill. Ledger: `issues/ledger.json`.*
